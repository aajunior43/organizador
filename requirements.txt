# RENOMER - Sistema de Organização de Extratos Bancários
# Requirements file

# Core dependencies
pathlib2>=2.3.7; python_version < '3.4'

# Flask API (opcional - apenas se usar a API web)
flask>=2.3.0
flask-cors>=4.0.0

# Google Gemini AI (opcional - apenas se usar IA)
google-generativeai>=0.3.0

# Utilities
python-dateutil>=2.8.2

# Logging com rotação
# (logging.handlers já vem no Python padrão, não precisa instalar)

# Desenvolvimento e testes
pytest>=7.4.0
pytest-cov>=4.1.0

# Documentação
# sphinx>=7.0.0  # Descomente se precisar gerar documentação